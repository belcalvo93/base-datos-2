# Secretos — nunca se versionan
.env

# Respaldos de base de datos (binarios, se regeneran)
food-store/backups/*.dump
food-store/backups/*.backup
*.dump
*.backup

# Dependencias y logs
node_modules/
*.log
